app-id: hu.irl.cameractrls
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
finish-args:
  - --device=all
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=/run/spnav.sock:ro
  - --env=SPNAV_SOCKET=/run/spnav.sock
command: cameractrlsgtk4.py
cleanup:
  - /include
  - /lib/cmake
  - /lib/pkgconfig
  - '*.a'
  - '*.la'
modules:
  - name: cameractrls
    buildsystem: simple
    build-commands:
      - install -D -t /app/bin camera{ctrls{,d,gtk,gtk4},view}.py
      - install -D -t /app/bin cameraptz{spnav,game,midi}.py
      - install -D -t /app/share/metainfo pkg/hu.irl.cameractrls.metainfo.xml
      - install -D -t /app/share/icons/hicolor/scalable/apps pkg/hu.irl.cameractrls.svg
      - install -D -t /app/share/applications pkg/hu.irl.cameractrls.desktop
    post-install:
      - python3 -m compileall /app/bin
    sources:
    - type: git
      url: https://github.com/soyersoyer/cameractrls
      tag: v0.6.10
      commit: d2770572513ed38b52e9eb72c8d0466aa53045a3
    modules:
      - name: libspnav
        config-opts:
          - --disable-debug
          - --disable-x11
        sources:
          - type: git
            url: https://github.com/FreeSpacenav/libspnav.git
            tag: v1.2
            commit: d641e5cbbe0d6d2381a2ee8d95c8c7273d005272
